{
  "_comment": "ILLUSTRATIVE — replace with live capture after `terraform apply`. The `_comment` field is not present in real gcloud output; remove this whole block before using as evidence. The shape of auditConfigs matches `gcloud projects get-iam-policy <project> --format=json` exactly. `auditLogConfigs` and `logType` are camelCase in the real output.",
  "auditConfigs": [
    {
      "service": "storage.googleapis.com",
      "auditLogConfigs": [
        {"logType": "DATA_READ"},
        {"logType": "DATA_WRITE"},
        {"logType": "ADMIN_READ"}
      ]
    },
    {
      "service": "cloudkms.googleapis.com",
      "auditLogConfigs": [
        {"logType": "DATA_READ"},
        {"logType": "DATA_WRITE"},
        {"logType": "ADMIN_READ"}
      ]
    },
    {
      "service": "iam.googleapis.com",
      "auditLogConfigs": [
        {"logType": "DATA_READ"},
        {"logType": "DATA_WRITE"},
        {"logType": "ADMIN_READ"}
      ]
    }
  ]
}